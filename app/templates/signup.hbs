{{#em-form model=model}}
{{em-input property="nickname" label="Nickname" placeholder="Nickname" class="nickname"}}
{{em-input property="email" label="Email" placeholder="Email" class="email"}}
{{em-input property="password" label="Password" placeholder="Password" class="password"}}
{{em-input property="password_confirmation" label="Password (confirmation)"
  placeholder="Reenter your password" class="password_confirmation"}}
{{/em-form}}
